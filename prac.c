function add(a: int, b: int) -> int {

	while (x < y && x > 1 && (y || x)) {
		let x: int = 3;
		arr[x] = 3;
		arr(3, 3);
	}


	for (x: int = 0; x < size; x++) {
		let w: int = 4;
	}
	
	return w + x;
}