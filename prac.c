

function add(a: int, b: int) -> int {
	let r: int = 10 / (2 * (2 * 3)) + (3 * 2) + 10 * 20 / 10;
	let z: int[y] = {1, 2, 3, 4};
	let w: int = 3;
	let y: int = 4;
	let x: int = 3;
	let y: int;

	let p: char = 'c';
	let t: bool = true;
	let arrr: bool[x] = {true, true, true};
	let char_array: char[x] = {'c', 'a', 't'};
	let x: int = mul(w, y);

	if (x < y) {
		let t: int = 3;
		let w: int[t] = {1, 2, 3,};
	} else if (x > y) {
		let y: int = 6;
		let w: char = 't';
		let pwd: bool = true;
		let p: char = 'c';
		let t: bool = true;
		let arrr: bool[x] = {true, true, true};
		let char_array: char[x] = {'c', 'a', 't'};
		let x: int = mul(w, y);

	}
	
	return w + x;
}