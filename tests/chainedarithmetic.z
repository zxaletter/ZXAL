function double_it(n: int) -> int {
	return n * 2;
}

function add_five_or_3(x: int) -> int {
	let greater_than_five: bool = x > 5;
	if (!greater_than_five) {
		return x + 5;
	}
	return x + 3;
}

function main() -> int {
	let result: int = add_five_or_3(double_it(3));
	return result;
}