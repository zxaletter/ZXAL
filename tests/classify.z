function square(x: int) -> int {
	return x * x;
}

function classify(n: int) -> int {
	if (n < 10) {
		return -1;
	} else if (n == 10) {
		return 0;
	} else {
		return 2;
	}
}

function main() -> int {
	let x: int = classify(10);
	return x;
}