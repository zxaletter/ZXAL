function lcm(a: int, b: int) -> int {
	return a / b;
}

function gcd(a: int, b: int) -> int {
	if (b == 0) {
		return a;
	}
	return gcd(b, a % b);
}

function main() -> int {
	let x: int = gcd(12, 38);
	let y: int = lcm(12 * 38, x);
	return 3;
}